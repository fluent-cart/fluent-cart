# FIX-PLAN.md — open bug/gap backlog

> Findings recorded by test runs. Nothing here is scheduled until a maintainer
> picks it up. Test-writing runs must ignore this backlog; parked
> `KNOWN-FAILURE` cases stay parked for a separate fix pass.

| # | Item | Kind | Risk |
|---|---|---|---|
| 1 | `KNOWN-FAILURE`: `GET reports/subscription-cohorts` emits a MySQL “table does not exist” error on the upgraded local install because `SubscriptionReportService` queries `fct_retention_snapshots` unconditionally at `app/Services/Report/SubscriptionReportService.php:364` and `:443`, while the migration is only defined at `database/Migrations/RetentionSnapshotsMigrator.php:5-45`. | Upgrade/migration gap | The admin cohort report can silently return empty data while WordPress emits a database error. |
| 2 | RESOLVED 2026-08-19 (TC-056, retirement): `GET reports/sales-growth` and its whole dead chain were removed — the route, `ReportingController::getSalesGrowth`, `app/Modules/ReportingModule/SalesReport.php`, and the never-imported `resources/admin/Charts/SaleGrowth.vue` (its only caller). The endpoint had returned no data since inception (`yearlySaleReport` was never assigned pre-refactor) and fataled since commit 982bf4233 removed the `OrderReportService::getModel()` override (and `getYearlySaleReport()` with it) — `generate()` → `buildQuery()` hits `''::search()` first; the live dashboard uses `reports/sales-growth-chart` instead. Zero consumers confirmed in fluent-cart-pro. | Runtime wiring defect — resolved by removal | — |
| 3 | `KNOWN-FAILURE`: `GET reports/quick-order-stats` fails to count a normal `payment_status=paid` Order as paid. `app/Modules/ReportingModule/OrdersReport.php:30-40` builds a filter on the Order `payment_status` column using `Status::getTransactionSuccessStatuses()`, which returns transaction values `succeeded` and `authorized` at `app/Helpers/Status.php:355-358`. The executable Phase 7 baseline-delta case observes exact deltas `total_orders=1`, `paid_orders=0`, `total_paid_order_items=0`, `total_paid_amounts=0` instead of `1/1/1/40000`. | Status-vocabulary mismatch | The deprecated quick-stats endpoint reports normal paid Orders as unpaid and omits their value. |
| 4 | `KNOWN-FAILURE`: stock reservation can exceed total stock. `StockManagement::manageStockOnOrderCreated()` clamps `available` to zero but increments `on_hold` by the full requested quantity at `app/Modules/StockManagement/StockManagement.php:168-181`. The executable Phase 8 case starts with total stock 3 and quantity 5, then observes `available=0`, `on_hold=5`, `committed=0`, and ledger total 5. | Stock conservation defect | Oversold Orders manufacture reserved stock beyond the variation's declared total. |
| 5 | `KNOWN-FAILURE`: canceled-Order restock is not idempotent. `StockManagement::manageStockOnOrderStatusChanged()` drains the recorded movement at `app/Modules/StockManagement/StockManagement.php:430-510` but always adds the full Order quantity to `available` at `:512-527`. The executable Phase 8 case starts at 10, reserves 2, restores to 10 once, then a repeated cancellation raises available stock to 12. | Lifecycle idempotency defect | Duplicate cancellation delivery can inflate sellable inventory. |
| 6 | `KNOWN-FAILURE`: scheduled actions are selected without an atomic claim. `JobRunner::start()` reads all matching `pending` rows and calls the worker at `app/Hooks/Scheduler/JobRunner.php:37-48` without changing status first. The executable Phase 8 case runs two recording workers and observes both select the same still-pending action ID. | Scheduler concurrency defect | Overlapping workers can process the same scheduled action more than once. |
| 7 | `KNOWN-FAILURE`: a title-only Variant admin update changes the physical `other_info` column. `ProductVariationResource::update()` reconstructs the submitted onetime metadata and then unconditionally appends default `is_bundle_product` and `bundle_child_ids` keys at `api/Resource/ProductVariationResource.php:241-281`. The executable Phase 9 route round-trip observes those two keys added while every submitted value remains the same except `variation_title`. | Silent unrelated-field mutation | An otherwise unrelated Variant edit rewrites stored metadata, violating lossless one-field update semantics and making future default changes capable of altering legacy rows. |
| 8 | `KNOWN-FAILURE`: Labels do not expose entity update or delete CRUD. The Labels route group at `app/Http/Routes/api.php:662-672` declares list, create, and relationship-selection update only; the nominal `LabelResource::update()` and `::delete()` implementations are empty at `api/Resource/LabelResource.php:122-140`. The executable Phase 9 source case verifies that exact missing shape. | Admin API gap | Administrators cannot rename or delete a Label through the REST entity API, so a complete Label create/read/update/delete round-trip is impossible. |
| 9 | `KNOWN-FAILURE` / `VALIDATION GAP`: shared list search input is assigned directly from request arguments at `app/Services/Filter/BaseFilter.php:231` instead of passing through `sanitize_text_field()`. The executable Phase 10 read-only probes wrap real Product, Order, Customer, and Activity search values in harmless `<b>` tags; every plain value is found and every tagged equivalent returns zero rows. | Missing text-input sanitizer | A refactor or downstream search implementation receives raw markup and control text, while callers cannot rely on the project-mandated normalized search contract. |
| 10 | `KNOWN-FAILURE` / `VALIDATION GAP`: LIKE helpers concatenate request values into wildcard patterns without `$wpdb->esc_like()` at `app/Models/Concerns/CanSearch.php:139-160`; direct list filters repeat the pattern at `app/Services/Filter/OrderFilter.php:43-51`, `CustomerFilter.php:25-32`, and `LogFilter.php:13-31`. Phase 10 compares literal-`%` counts from read-only escaped SQL with the REST results and observes the wildcard matching every Product, Order, Customer, and Activity row. | Missing LIKE escaping | A user-supplied `%` or `_` changes query semantics from literal search to an unbounded wildcard scan and can expose unexpectedly broad result sets. |
| 11 | `KNOWN-FAILURE` / `VALIDATION GAP`: `ActivityController` returns stored Activity content unchanged at `app/Http/Controllers/ActivityController.php:11-16`, while the Order Activity UI renders it directly through `v-html` at `resources/admin/Modules/Orders/Activity.vue:18` (and Dashboard activity does the same at `resources/admin/Pages/Dashboard/Components/RecentActivity.vue:162`). The Phase 10 source inventory pins 15 direct stored/row-derived `v-html` sinks, and its read-only exact-ID probe observes raw stored markup rather than escaped output. | Unsafe stored-output rendering | Markup stored in Activity or other row-derived fields can become executable DOM content in an administrator or customer browser. |
| 12 | `KNOWN-FAILURE` / `VALIDATION GAP`: list endpoints that bypass `BaseFilter` pass raw `per_page` values into paginators. The executable read-only `GET public/products` probe observes `per_page=100000` preserved through `api/Resource/ShopResource.php:260-262`; the same unbounded flow is source-pinned at `app/Http/Controllers/FrontendControllers/CustomerOrderController.php:62-82`. The customer Order list is not dynamically invoked because the installed pre-existing row shape raises an unrelated `app/Models/Order.php:356` null-trim deprecation, which Phase 10 refuses to suppress or repair. | Missing pagination clamp | Oversized page requests can force large queries and response serialization work, creating an avoidable resource-exhaustion path. |
| 13 | `KNOWN-FAILURE`: duplicate delivery of an exhausted system-charge attempt is not idempotent. `SystemChargeService::executeCharge()` reaches `handleFailure()` at `app/Modules/Subscriptions/Services/SystemChargeService.php:477-528` and `:669-744` without first claiming or recognizing the already-recorded `(order_id, attempts, exhausted)` terminal state. The executable Phase 14 case delivers attempt 4 twice and observes two `fluent_cart/subscriptions/system_charge_failed` dispatches plus two “Automatic charge failed” Activities. Add an atomic per-order/per-attempt claim or a terminal-state early return before any failure log/hook, while preserving legitimate later attempts. | Background-delivery idempotency defect | Action Scheduler redelivery can duplicate customer-facing extension work and audit history for the same exhausted payment attempt. |
| 14 | `KNOWN-FAILURE`: `PUT customer-profile/orders/{transaction_uuid}/billing-address` dereferences `$transaction->order_id` at `app/Http/Controllers/FrontendControllers/CustomerOrderController.php:500-503` before checking whether the UUID resolved and before the customer-owned Order query at `:508`. The executable Phase 18 case observes a PHP warning for a syntactically valid unknown UUID while exact snapshots prove no row changed. Add a not-found return before dereference; retain the existing customer-owned Order guard before validation or writes. | Unsafe missing-object guard ordering | A malformed or guessed public transaction handle can raise a runtime diagnostic or 500-class response instead of a clean denial. |
| 15 | `KNOWN-FAILURE`: customer Subscription detail rejects only `pending` and `intended` at `app/Http/Controllers/FrontendControllers/CustomerSubscriptionController.php:96-108`; a `canceled` row with a valid UUID continues into gateway-capability formatting. The Phase 18 source-pinned case does not dispatch the correct UUID because that would resolve gateway paths. Decide whether cancelled subscriptions must become inaccessible or whether the public-surface contract should explicitly preserve read-only history, then align the route and test. | Cancelled-handle authorization/product-policy gap | A cancelled record UUID remains usable beyond the roadmap's terminal-handle boundary and can enter gateway-dependent formatting. |
| 16 | `KNOWN-FAILURE`: `Helper::toCent()` converts decimal input through `floatval()` before rounding at `app/Helpers/Helper.php:577-578`. The executable Phase 12 boundary case observes `'1.005' → 100` and `'-1.005' → -100` instead of the mathematically exact `101/-101`. Replace the binary-float conversion with a decimal-safe cents conversion that preserves the PHP 7.4-8.4 contract and define overflow behavior explicitly. | Money precision defect | Half-cent decimal inputs can round one cent toward zero, producing incorrect stored totals or adjustments at money-ingress boundaries. |
| 17 | Phase 20 strict-SQL finding: `ReportHelper::processGroup()` selects both `DATE_FORMAT(o.created_at, ...) AS date_group` and `YEAR(o.created_at) AS year` at `app/Services/Report/ReportHelper.php:55-56`, but groups only by the formatted expression. With session-level `ONLY_FULL_GROUP_BY`, the existing suite fails 17 REST report variants and four integration report cases because `YEAR(o.created_at)` is neither grouped nor aggregated. Make every selected period expression functionally valid under strict grouping without changing response keys or date-bucket semantics. | Strict-SQL report incompatibility | Revenue, Order, sales, subscription, product, and customer report surfaces can fail on MySQL installations that enforce production-default grouping rules. |
| 18 | Phase 20 strict-SQL finding: the repeat-customer constraint in `CustomerHelper::getCustomerQuery()` applies `HAVING COUNT(*) > 1` inside the Order relation subquery at `app/Helpers/CustomerHelper.php:101-108` without a corresponding `GROUP BY`. The existing `GET reports/search-repeat-customer` smoke case fails under `ONLY_FULL_GROUP_BY`. Reshape the existence/count query while preserving its “more than one Order” contract. | Strict-SQL aggregate incompatibility | Repeat-customer search is unavailable on strict MySQL sessions. |
| 19 | Phase 20 strict-SQL finding: `ProductReportService` selects Order-item and post titles in a grouped subquery at `app/Services/Report/ProductReportService.php:11-13`, then selects Variant/title fields in the outer grouped result at `:25-36`, while neither grouping covers every nonaggregated selected column. The existing Product-report cases fail under `ONLY_FULL_GROUP_BY` on `fct_order_items.title`. Make title selection deterministic for each grouped identity without changing report shape. | Strict-SQL product-report incompatibility | Product sales reports can fail entirely on strict MySQL sessions. |
| 20 | Phase 20 strict-SQL finding: `SourceReportService` selects `utm_term`, `utm_content`, and row identity while grouping only by campaign/source/medium at `app/Services/Report/SourceReportService.php:12-31`. The existing Source report smoke case fails under `ONLY_FULL_GROUP_BY` on `utm_term`. Define deterministic aggregation or extend the grouping according to the intended attribution granularity. | Strict-SQL source-report incompatibility | Marketing attribution reports can fail on strict MySQL sessions or return ambiguous representative values on permissive sessions. |
| 21 | Phase 20 strict-SQL finding: `BulkProductUpdateService` calls `get_the_terms()` while building bulk-edit data at `app/Services/BulkProductUpdateService.php:104-105` and `:127-129`. WordPress then issues a taxonomy query that selects distinct term IDs while ordering by a non-selected term name, which MySQL rejects under `ONLY_FULL_GROUP_BY`. The existing `GET products/bulk-edit-data` smoke case exposes the incompatibility. Introduce a strict-compatible taxonomy lookup/order boundary without changing the endpoint response. | Strict-SQL WordPress-query incompatibility | Product bulk-edit initialization can fail when the store connection enforces strict grouping rules. |
| 22 | Phase 20 non-UTC finding: `OrderItemResource::update()` writes `updated_at` with framework `DateTime::now()` at `api/Resource/OrderItemResource.php:270`. At the hostile `gmt_offset=-4.5` axis, the existing safe Order route round-trip observes the automatic timestamp move backwards relative to the row’s UTC database timestamp. Store the timestamp in the schema’s canonical timezone and preserve monotonic update behavior. | Timezone storage mismatch | Order-item edits on negative-offset stores can persist an `updated_at` value hours earlier than the prior database value, breaking ordering and synchronization assumptions. |
| 23 | `KNOWN-FAILURE`: `CanCalculateLineTotal::distributeDiscount()` rounds proportional shares as fractional cents at `app/Services/Coupon/Concerns/CanCalculateLineTotal.php:131-136`, while `calculateLineItems()` later casts every line discount independently to an integer at `:35-37`. The executable Phase 23 case applies a 1001-cent fixed coupon across 3333/3333/3334-cent lines and observes line discounts sum to 1000 cents while the coupon ledger records 1001. Allocate integer cents with a deterministic remainder rule so line sums equal the coupon/order discount exactly. | Coupon cent-conservation defect | Multi-line orders can retain an undistributed cent, so line totals disagree with the stored coupon and payable-order totals. |
| 24 | `KNOWN-FAILURE`: the Phase 26 browser smoke reaches the registered `view_customer_licenses` route with a temporary WordPress administrator that has `manage_options`, but `GET fluent-cart/v2/licensing/licenses/customer/{id}` returns `403`. Other license routes mount under the same session. Review the pro `LicensePolicy` mapping for this customer-license action without broadening unrelated licensing permissions. | License-policy mismatch | Administrators cannot open the registered customer-license detail screen. |
| 25 | `KNOWN-FAILURE`: `resources/admin/Modules/Products/Integrations/SingleIntegration.vue` requests `GET products/{product}/integrations/{numeric_id}`, but the core API registers the settings read as `GET products/{product}/integrations/{integration_name}/settings`; no numeric-ID GET route exists. The Phase 26 browser smoke observes the registered `SingleIntegration` screen issue a `404`. Align or remove the stale screen route while preserving the supported integration-name contract. | Stale frontend/API contract | Opening the registered per-product integration detail screen produces a failed request and no usable settings view. |
| 26 | `KNOWN-FAILURE`: the registered `integration_editor` screen requests global integration settings for a route key, but this install/repository has no matching `fluent_cart/integration/global_integration_fields_*` registration and the request returns `422`. Determine whether the editor is a legacy route to remove or restore its integration field provider before un-parking the Phase 26 browser case. | Unwired legacy route | A registered admin route cannot obtain the schema required to render its editor. |
| 27 | `KNOWN-FAILURE` / `COVERAGE GAP`: the protected local store has zero `fct_shipping_classes` rows, so the Phase 26 read-only browser tier cannot construct a valid `view_shipping_class` detail URL without creating data. The add-shipping-class screen is still exercised. Supply a committed disposable fixture strategy or a seeded non-production class before un-parking the detail route. | Missing read-only browser fixture | The shipping-class detail component is not exercised by the read-only every-screen mount tier. |
| 28 | `KNOWN-FAILURE` / `FLAKE`: the Phase 26 every-screen smoke intermittently raises `Cannot read properties of undefined (reading 'offsetHeight')` during sequential SPA navigation. It first appeared at `/settings/invoice-packing`, passed on an unchanged rerun, and then appeared at `/settings/licensing`; it is therefore not parked as a route-specific failure. Phase 28 keeps every eligible screen mounted, fails all other uncaught errors, and explicitly parks only this exact Element Plus diagnostic instead of reporting a flaky all-clear. Stabilize the shared layout lifecycle and prove repeated runs without the diagnostic before removing the park. | Intermittent cross-route browser lifecycle error | A registered admin screen can emit an uncaught page error depending on navigation timing; the exact diagnostic remains visible but is excluded from the deterministic browser gate. |
| 29 | `KNOWN-FAILURE` / `TEST-INFRA FLAKE`: the local site uses a custom `/account/` authentication redirect, while the shared browser helper at `dev/browser/setup.mjs:492-498` waits only for a `wp-admin` URL. The Phase 26 disposable administrator usually authenticates through the custom page, but audit attempts have timed out before route coverage even when WordPress later exposed an authenticated page. The smoke tier records a skip whenever helper completion cannot be trusted, verifies the exact owned user through WP-CLI, installs one-hour WordPress `secure_auth`/`logged_in` cookies only for that user when needed, and still requires independently verified authentication plus complete route coverage; a failed fallback remains fatal. Replace the fallback with a deterministic shared login contract that handles custom redirects before removing the skip. | Intermittent local browser authentication handshake | A browser evidence attempt can stop before route coverage even though fixture cleanup remains exact; recovered runs are no longer reported as unqualified green. |
| 30 | `KNOWN-FAILURE`: Stripe webhook delivery does not verify `Stripe-Signature`. `StripeGateway\API\API::verifyIPN()` at `app/Modules/PaymentMethods/StripeGateway/API/API.php:113-128` only decodes the body and requires an event ID; `StripeGateway\Webhook\IPN::verifyAndProcess()` at `app/Modules/PaymentMethods/StripeGateway/Webhook/IPN.php:401-429` then fetches that ID from Stripe without checking an endpoint secret or signature header. The executable Phase 29 case supplies the same canned event with absent, wrong, and malformed signatures and observes all three accepted as `evt_phase29_stripe_charge_1`. Add Stripe's timestamped HMAC verification against the mode-specific webhook secret before any event lookup or local dispatch, reject missing/malformed/stale signatures, and preserve the existing canonical event fetch only as a second validation layer. | Missing webhook authentication | Anyone who learns a valid Stripe event ID can trigger the store's webhook lookup and downstream processing path without possessing a valid endpoint signature. |
| 31 | `KNOWN-FAILURE`: the admin `Model` proxy drops falsy store values. `resources/admin/utils/model/Model.js:45-49` joins `#resolveProperty()` and `#resolveRestMethod()` with `||`, so legitimate `false`, `0`, and `''` values fall through to an undefined REST method and read back as `undefined`. The executable Phase 31 case observes all three values lost. Resolve by property existence rather than truthiness, while preserving dynamic `$get`/`$post` delegation. | Falsy state read corruption | Admin state models cannot reliably represent disabled flags, zero counts, or intentional empty strings through their public proxy. |
| 32 | `KNOWN-FAILURE`: checkout rejects valid plus-address email local parts. `resources/public/checkout/validateCheckout.js:61` uses `^\\w+([\\.-]?\\w+)*`, which excludes `+`; the executable Phase 31 case rejects `shopper+receipts@example.com` with `validation_failed`. Replace the ad-hoc pattern with a browser-compatible validation contract that accepts valid plus addressing without admitting malformed domains. | Over-restrictive email validation | Shoppers using tagged/plus addresses cannot pass checkout billing-email validation. |
